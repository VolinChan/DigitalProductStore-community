import LegalDocument from '@/components/legal/LegalDocument';
import SupplierDisclosure from '@/components/legal/SupplierDisclosure';
import { supplierProfile } from '@/lib/legal/supplier';
export default async function SupplierPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const es = locale !== 'en'; return <LegalDocument locale={locale} title={es ? 'Identificación del proveedor' : 'Supplier identification'}><SupplierDisclosure locale={locale} /><section><h2 className="text-xl font-black">{es ? 'Actividad declarada' : 'Declared activity'}</h2><p>{es ? 'Venta al por menor por correo, por Internet y vía telefónica (SII 479100). Inicio de actividades informado: 18 de diciembre de 2025.' : 'Retail sales by mail, internet, and telephone (SII 479100). Reported activity start date: December 18, 2025.'}</p><p className="mt-3">WhatsApp: <a className="underline" href={supplierProfile.whatsappHref}>{supplierProfile.phoneDisplay}</a></p></section></LegalDocument>; }
