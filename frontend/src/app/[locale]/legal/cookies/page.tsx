import LegalDocument from '@/components/legal/LegalDocument';
export default async function Cookies({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const es = locale !== 'en'; return <LegalDocument locale={locale} title={es ? 'Política de cookies y almacenamiento' : 'Cookie and storage policy'}><section><h2 className="text-xl font-black">{es ? 'Categorías' : 'Categories'}</h2><p>{es ? 'PLEXORIA clasifica el almacenamiento como necesario, análisis, marketing o personalización. Sesión de carrito, autenticación, seguridad y preferencia de idioma pueden operar como necesarios.' : 'PLEXORIA classifies storage as necessary, analytics, marketing, or personalization. Cart session, authentication, security, and language preference may operate as necessary.'}</p></section><section><h2 className="text-xl font-black">{es ? 'Control del visitante' : 'Visitor control'}</h2><p>{es ? 'Los proveedores no esenciales no se inician antes del consentimiento correspondiente. El centro de preferencias permite aceptar, rechazar o personalizar y puede abrirse nuevamente desde el pie de página. Mientras no exista una elección, solo funcionan las categorías necesarias.' : 'Nonessential providers do not initialize before the corresponding consent. The preference center allows visitors to accept, reject, or customize and can be reopened from the footer. Until a choice is made, only necessary categories operate.'}</p></section></LegalDocument>; }
